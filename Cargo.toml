[workspace]

members = [
    "lightning",
    "lightning-net-tokio",
    "lightning-block-sync",
]

# Our tests do actual crypo and lots of work, the tradeoff for -O1 is well worth it
[profile.dev]
opt-level = 1
